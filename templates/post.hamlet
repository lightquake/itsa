<article>
  <div>
    <a .title href=@{routePost post}>#{view title post}
  <div>
    <div .content>#{view body post}
    <div .metadata>
     <span .time>
        Posted: #{formatTime defaultTimeLocale "%B %e, %Y" $ utcToZonedTime tz $ view posted post}

     <span .tags>
        $with tags <- view tags post
          $if not (null tags)
            Tagged: <a href="@{TagR (head tags) 1}">#{head tags}</a>
            $forall tag <- tail tags
              , <a href="@{TagR tag 1}">#{tag}</a>

     <div style="clear: both;">
